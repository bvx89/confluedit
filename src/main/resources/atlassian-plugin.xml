<atlassian-plugin key="${project.groupId}.${project.artifactId}" name="${project.name}" plugins-version="2">
    <plugin-info>
        <description>${project.description}</description>
        <version>${project.version}</version>
        <vendor name="${project.organization.name}" url="${project.organization.url}" />
        <param name="plugin-icon">images/pluginIcon.png</param>
        <param name="plugin-logo">images/pluginLogo.png</param>
    </plugin-info>

    <!-- add our i18n resource -->
    <resource type="download" name="images/" location="/images"/>
    
    <!-- add our web resources -->
    <web-resource key="confluedit-resources" name="confluedit Web Resources">
        <dependency>com.atlassian.auiplugin:ajs</dependency>
        
        <resource type="download" name="confluedit.css" location="/css/confluedit.css"/>
        <resource type="download" name="confluedit.js" location="/js/confluedit.js"/>
        <resource type="velocity" name="template" location="/templates/editor-macro.vm"/>

        <context>confluedit</context>
    </web-resource>

    <!-- publish our component -->
    <component key="editorPluginComponent" class="no.ntnu.assignmentsystem.confluedit.EditorPluginComponentImpl" public="true">
        <interface>no.ntnu.assignmentsystem.confluedit.EditorPluginComponent</interface>
    </component>


    <!-- import from the product container -->
    <component-import key="applicationProperties" interface="com.atlassian.sal.api.ApplicationProperties" />

    <xhtml-macro name="editor" class="no.ntnu.assignmentsystem.confluedit.EditorMacro" key="editor">
        <description>Inserts a live editor into the current page</description>
    </xhtml-macro>

    <rest name="My Rest Resource" key="my-rest-resource" path="/editorinit" version="1.0">
        <description>The My Rest Resource Plugin</description>
    </rest>
</atlassian-plugin>